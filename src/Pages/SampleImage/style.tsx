import styled from "styled-components";

const WrapperDiv = styled.div``;

const StyledImage = styled.img`
  width: 500px;
  height: 500px;
  object-fit: cover;
`;

export default {
  WrapperDiv,
  StyledImage,
};
